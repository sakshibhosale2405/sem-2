#include <stdio.h>

// DECLARING FUNCTION BEFORE THE MAIN FUNCTION

int sum(int a, int b)
{
    return a+b;
}
int main()
{
    int a,b,c;
    printf("enter value of a and b:");
    scanf("%d%d",&a,&b);
    c=sum(a,b);
    printf("the sum of %d and %d is %d\n", a,b,c);
    
}
