#include <stdio.h>

int main() {
    int a;
    printf("enter value of a: ");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("no is even no ");
        
    }
    
  
    else
    {
        printf("no is odd no");
    }
    return 0;
}
